/**
 * @file [locale]/opengraph-image.tsx
 * @description Locale-level OG image — re-exports the root design so every
 * locale page (e.g. /en, /fr, /ar) gets the same branded social card.
 */

export { alt, size, contentType, default } from "../opengraph-image";
